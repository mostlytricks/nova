# Example API

A placeholder. Replace with notes about one of your own APIs.

Use this page for local API knowledge that should be available to coding agents
before they search external documentation. Include authentication rules, base
URLs, request and response shapes, known edge cases, and operational cautions.
When the API grows into a real project area, move this file into a dedicated
namespace and update the namespace manifest.

## Endpoints

- `GET /things` - list things
- `POST /things` - create a thing

## Agent Notes

- Prefer examples that can be copied into tests or client code.
- Record any headers, cookies, or tenant-specific parameters needed locally.
- Keep deprecated behavior visible until dependent projects are migrated.
