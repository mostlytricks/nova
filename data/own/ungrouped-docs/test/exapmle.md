# test/exapmle.md

This is a scratch note kept under the `ungrouped-docs` namespace so it is still
visible to the local `llms.txt` server without pretending to be a stable
project namespace. Use this area for temporary docs, experiments, and notes
that should be available to an agent during development.

If this note becomes important, rename it with a clear filename, add a concise
description in `ungrouped-docs/llms.txt`, and eventually move it into a
dedicated namespace. The misspelled filename is preserved from the original
root-level test entry so the migration does not silently change user content.
